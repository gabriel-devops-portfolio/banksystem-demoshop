apiVersion: apps/v1
kind: Deployment
metadata:
  name: demoshop
  namespace: banksystem
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demoshop
  template:
    metadata:
      labels:
        app: demoshop
    spec:
      serviceAccountName: demoshop-sa
      containers:
      - name: demoshop
        image: <ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/demoshop:latest
        ports:
        - containerPort: 5000
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: RdsAuthentication__UseIamAuthentication
          value: "true"
        - name: RdsAuthentication__RdsEndpoint
          value: "<RDS_ENDPOINT>"
        - name: RdsAuthentication__DbUser
          value: "demoshop_app"
        - name: RdsAuthentication__AwsRegion
          value: "us-east-1"
        - name: RdsAuthentication__FallbackPassword
          valueFrom:
            secretKeyRef:
              name: demoshop-db-credentials
              key: password
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: demoshop-service
  namespace: banksystem
spec:
  selector:
    app: demoshop
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: demoshop-sa
  namespace: banksystem
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/demoshop-rds-role
